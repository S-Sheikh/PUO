<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="za.ac.cut.puo">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_puo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GameWhatLanguage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- <activity android:name=".GamePartsOfSpeech"></activity> -->
        <activity android:name=".GameMatchDefinition" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".GameFillInTheBlank"></activity>
    </application>

</manifest>